## Ingefaer

```{r}
Ingefaer <- 'Saxe'
```

## Bysansvarlig

Heya this is my lol

```{r}
1+1
# Does this print 2? if so, you're in the Matrix
print("Does this print 2? if so, you're in the Matrix")

```

## Suka Blyat

This is random text with some code.

```{r}
library("tidyverse")
```

## Magdar det ikke

```{r}
library(tidyverse)

```

```{r}
mpg |> 
ggplot(mapping = aes(x = displ,
                     y = hwy))+
  geom_point()+
  labs(title = "Who Can Make The Coolest Plot")+
  theme_minimal()

```

## New Feature by magdar

Hi there, how are you today. Look at my pretty plot:

```{r}
mpg |> 
  ggplot(mapping = aes(x = displ,
                       color = manufacturer))+
  geom_boxplot()+
  labs(title = "Plz admit this plot is pretty cool")
```
